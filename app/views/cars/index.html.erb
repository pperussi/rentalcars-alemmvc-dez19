<h1>Carros</h1>
<%= link_to 'Registrar novo carro', new_car_path, class: 'btn btn-primary' %>
<% if @cars.any? %>
  <ul>
  <% @cars.each do |car| %>
    <li><%= Car.human_attribute_name('license_plate') %>: <%= link_to car.license_plate, car_path(car) %></li>
  <% end %>
  </ul>
<% end %>
